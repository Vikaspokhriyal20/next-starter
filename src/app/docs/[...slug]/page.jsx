import React from 'react'

const page = ({ params }) => {
  if (params.slug.length==2) {
    
  }
  return (
    <div className='container'>
      <p>This is Docs </p>
    </div>
  )
}

export default page
